spring:
  datasource:
    url: jdbc:h2:mem:course_platform;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false

management:
  health:
    redis:
      enabled: false
